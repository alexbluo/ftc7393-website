const parseContent = (page: string) => {
  // get image and caption
  // sponsor might need hard coding
  // have to all be in public folder, include page in file name
  // how to run server side?
};

export default parseContent;
